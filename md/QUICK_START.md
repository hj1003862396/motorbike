# 快速参考指南

## ⚡ 最快开始（3 步）

### 1️⃣ 查看效果
```bash
open output_detection.mp4
```

### 2️⃣ 处理新视频
```bash
export https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890 all_proxy=socks5://127.0.0.1:7890
python3 save_output_video.py
```

### 3️⃣ 查看新结果
```bash
open output_detection.mp4
```

---

## 📋 命令速查表

| 任务 | 命令 |
|------|------|
| 查看输出视频 | `open output_detection.mp4` |
| 处理视频（推荐） | `python3 save_output_video.py` |
| 快速统计 | `python3 run_video.py` |
| 演示版本 | `python3 demo_run.py` |
| 测试环境 | `python3 test_setup.py` |
| 完整项目 | `python3 motorbike_project.py` |

---

## 🎯 根据需求选择

### 我想看检测效果
```bash
open output_detection.mp4
```

### 我想处理自己的视频
1. 将视频放在项目目录
2. 编辑 `save_output_video.py`，改视频文件名
3. 运行：`python3 save_output_video.py`

### 我想快速测试
```bash
python3 test_setup.py
```

### 我想看统计数据
```bash
python3 run_video.py
```

---

## 🔧 修改视频文件名

编辑 `save_output_video.py`，找到第 XX 行：

```python
cap = cv2.VideoCapture("a.mp4")  # ← 改这里
```

改为你的视频文件名：

```python
cap = cv2.VideoCapture("your_video.mp4")  # ← 改成你的文件名
```

---

## 📊 理解输出

### 视频中的标记

- **绿色框** = 检测到的对象
- **ID: 1, 2, 3** = 跟踪 ID
- **motorbike** = 对象类别
- **绿色网格** = 99 个网格单元

### 统计数据

```
Frame: 5/12              # 当前帧 / 总帧数
Detections: 2           # 当前帧检测数
Tracked IDs: 2          # 当前帧跟踪数
```

---

## ⚙️ 调整参数

### 提高检测准确度

编辑 `save_output_video.py`：

```python
od.load_detection_model(
    image_size=416,        # 改大 → 更准确但更慢
    confThreshold=0.3      # 改小 → 检测更多
)
```

### 改进跟踪效果

```python
deep = Deep(
    max_distance=0.7,      # 改小 → 跟踪更严格
    max_age=15,            # 改大 → 保持轨迹更久
)
```

---

## 🐛 常见问题

| 问题 | 解决方案 |
|------|---------|
| 找不到视频 | 检查文件名和位置 |
| 模型加载失败 | 检查 yolov4.weights 是否存在 |
| SSL 错误 | 使用代理设置运行 |
| 输出视频无法打开 | 用不同播放器试试 |

---

## 📁 文件位置

```
/Users/hanjie/PycharmProjects/Motorbike-detection/
├── save_output_video.py      ← 推荐使用
├── run_video.py              ← 快速处理
├── motorbike_project.py      ← 完整项目
├── yolov4.weights            ← 模型（246 MB）
├── yolov4.cfg                ← 配置
├── coco.names                ← 类别
├── a.mp4                      ← 输入视频
└── output_detection.mp4      ← 输出视频
```

---

## 💡 提示

- 第一次运行会加载模型，可能需要几秒钟
- 输出视频大小取决于输入视频长度
- 可以处理任何 MP4 格式的视频
- 按 ESC 可以退出实时显示版本

